# 待你 Review 的决策点

> 按用户要求：执行过程中遇到的权衡决策点、模糊地带、优化建议，先认真思考给出
> 倾向性判断，再记录在这里，不打断当前工作，供你之后集中审阅。
> 最后更新：2026-07-26

---

## 需要你明确表态的

### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少

新增 `sector_tags`/`entities`/`sentiment`/`impact_horizon` 后，实测单条成本从
$0.0116 涨到 $0.0151（+30.1%）。按当前节奏（12h/轮、约 490 条/轮、2 轮/天）
换算，**约 +$103/月**。

**我的判断**：这个增量是必要的——"真相关才打"和市值标签都是你明确要求的，
量化相关度（`anchor` 锚点强制要求）已经让过度打标从 14 个板块降到 6 个，效果
实打实。$103/月相对整个项目的价值不算高。**倾向于接受**，除非你有更严格的
预算线。

**如果需要继续压成本**：最大的可砍项不是这批新字段，是 `description_long_en/zh`
（中英各 200-400 字），这两个字段占 output token 的大头。要砍的话砍这个，
不是砍刚加的标签。

**2026-07-26 更新——现在成本大概多少**：已经做了 description_long 精简 +
模型分级（见下面 4b）。真实生产数据：优化前单事件 $0.01456（run 11），优化后
单事件 $0.00925（run 12），**降了 36.5%**。按当前节奏（12h/轮、2轮/天，典型
单轮 500-800 事件）估算，OpenAI 这块月成本大概在 **$280-450/月**区间（用量
波动大，这是预估不是精确值——按你的最新反馈，这类统计预估级别就够）。
这还不含 X API 那部分（见下面 #3 的更新）。


### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少
### 2. API 生产化程度够不够   -- 够用。token生成5个不同的吧，我后面可以给不同的人

你要求"输出一个可以用的API，注意你给的API就是生产化的"。现状：
- ✅ 功能完整、字段丰富、有文档（工程师版 + 非技术版）、公网可用
- ⚠️ **HTTP 明文，无 TLS**——token 和数据在传输中可被窃听
- ⚠️ **单一静态 token，无分级权限、无速率限制**——任何拿到 token 的人可读全部数据
- ⚠️ 单实例无高可用

**我的判断**：作为"能用、可以给下游对接"的生产 API，现状够格；作为"能扛外部
攻击面"的生产 API，还差一截。**如果这个 API 只在公司内网/VPN 后面用，现状可以
接受；如果要暴露到公网给不受信任的调用方，建议加 HTTPS（Caddy/Nginx 反代 +
Let's Encrypt，工作量不大）**。要不要做，取决于实际的网络暴露面，需要你判断。

### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少
### 2. API 生产化程度够不够   -- 够用。token生成5个不同的吧，我后面可以给不同的人

### 3. X API 用量策略要不要现在重构  -- 先简单重构一下，降低点成本。昨天用了30$了。

原始要求是"API 只查询高相关的材料，而不是乱爬"，当前 22 组查询本质仍是预设
关键词的广撒网（虽然比裸查询精准）。你后来说"这个还好，后面看用量再调节吧"。

**我的判断**：保持现状到你实际购买 API、看到真实额度和账单后再重构是对的——
现在重构是没有真实反馈信号的过早优化。**这条先搁置，不用现在处理**，但已经
记录在 `docs/PROJECT_PLAN.md` 待办里，不会忘。

**2026-07-26 更新**：你反馈"昨天用了30$"，做了个轻量调整（`crawler/x_search.py`
的既定调参方向，注释里早就写好了"成本超标先调这两个"）：`market`/`macro` 两类
的最低互动量阈值从 25 提到 40（这两类本来就是噪音最大、召回价值相对最低的），
单轮下游输出上限从 200 条降到 150 条。**没动** `security`/`risk` 这些抢先性
优先的高价值类别。注意：代码里原有注释写着 X 的 search/recent 端点在配额内是
固定层级费用，不按调用量计费——如果你看到的 $30 确实是 X 官方账单里的实际扣费
（不是别的服务的费用被算到一起了），说明你的实际计费方式可能和这条假设不一致
（比如买的是按量计费的层级），这个我没法从我这边确认，需要你看一眼 X 开发者
后台的账单明细确认一下，如果计费模型不一样，调整方向可能要变。

---

## 模糊地带 / 需要你补充信息的


### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少
### 2. API 生产化程度够不够   -- 够用。token生成5个不同的吧，我后面可以给不同的人

### 3. X API 用量策略要不要现在重构  -- 先简单重构一下，降低点成本。昨天用了30$了。
### 4a. AB 对比工具的三个已知局限（Agent 交付时自评，非阻断项） 

评测工具合并进主页 + AB对比子tab（任务 #31）已交付并在 VM 验证跑通。交付 agent
自己指出三点，记录在这里供你判断要不要现在处理：  -- 不需要现在处理

1. **预览和实际对比之间可能有数据漂移**：DB 一键填充目前是"预览查一次、提交对比
   再按同样条件查一次"，如果两次查询之间数据库有新写入，两边可能对不上。
   **我的判断**：低概率场景（评测工具的使用节奏远慢于数据写入间隔），先不修，
   出现过一次真实数据不一致再改成"预览结果的 event_id 直接传给对比接口"。
2. **GSB 最近邻配对允许多对一**：A、B 条数差异大时，B 里同一条可能被多个 A
   条目匹配为"最相似对应物"。重合度判定（Jaccard）用的是严格贪心一对一匹配，
   不受影响，只有 GSB 展示的配对关系可能有一条 B 被引用多次。
   **我的判断**：这是显示层的可读性问题，不影响重合度这个核心指标的正确性，
   先不用处理，等你实际用起来觉得配对展示困惑再加提示。
3. **跨来源质量分不完全同口径**：数据库来源的 importance_score/credibility_score
   是真实字段，text/image 来源是 LLM 现场估计的，两者用同一 rubric 但终究不是
   同一种测量方式，直接比较均值有一点点"苹果对橙子"。前端已经在表格标题注明  
   数据来源，**我的判断**：这个免责说明够用，暂不需要更强的提示。  

### 4. 【已完成·含一次失信记录】Sector Insight 真实相关性算法

你已经明确要求实现（2026-07-26 追加需求，参考上传的 skill 文档），agent 正在
`crawler/sector_relevance.py` + `api/sector_insight.py` 实现两层设计（embedding+
关键词非LLM预筛 → LLM精细判定 → 硬门+打分+去重+宁缺毋滥）。完成后我会：
1. 注册新端点 `GET /api/recommend/sector`
2. **把策略实验室（`web/lab.html`）里的简化版二元 Rel 换成真实算法**（这是
   当初标注"简化版"的直接后续，届时会一并处理，不用你再提醒）

**2026-07-26 深夜闭环记录**：第 1 条当天完成；**第 2 条没有兑现**，直到用户
从界面上看到"尚未上线"的提示亲自追问才补做——正是这句"不用你再提醒"的承诺
失信了。现已完成：实验室 Rel 改为连续分（优先读入库时 LLM 打的 sector_relevance
明细，覆盖 86%；旧行按 sectors 0.60/成分币白名单 0.55/标题关键词 0.40 分层退化，
查询零 LLM 成本），数据层与浏览器层都已实测（MEME 板块相关性优先模式 Top6 =
0.93/0.88/0.86/0.79/0.72 连续分布）。教训入 memory `definition-of-done-user-surface`。




### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少
### 2. API 生产化程度够不够   -- 够用。token生成5个不同的吧，我后面可以给不同的人

### 3. X API 用量策略要不要现在重构  -- 先简单重构一下，降低点成本。昨天用了30$了。
### 4a. AB 对比工具的三个已知局限（Agent 交付时自评，非阻断项） 

评测工具合并进主页 + AB对比子tab（任务 #31）已交付并在 VM 验证跑通。交付 agent
自己指出三点，记录在这里供你判断要不要现在处理：  -- 不需要现在处理
### 5. 存量 416 行删除已执行，是否需要保留更久的备份 -- 其余的保留吧。一定阶段后导出存到VM硬盘里。也在本机存一份

执行 `backfill_dedup.py --apply` 前做了 `mysqldump` 备份（VM
`~/crypto-news-crawler/backups/news_events_20260725-212337.sql`，4.3MB）。
**这个备份目前只在 VM 本地，没有下载到别处，也没设过期清理策略**。如果 VM
出问题这份备份也会丢。要不要下载一份到本地或云存储，你可以决定。


### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少
### 2. API 生产化程度够不够   -- 够用。token生成5个不同的吧，我后面可以给不同的人

### 3. X API 用量策略要不要现在重构  -- 先简单重构一下，降低点成本。昨天用了30$了。
### 4a. AB 对比工具的三个已知局限（Agent 交付时自评，非阻断项） 

评测工具合并进主页 + AB对比子tab（任务 #31）已交付并在 VM 验证跑通。交付 agent
自己指出三点，记录在这里供你判断要不要现在处理：  -- 不需要现在处理
### 5. 存量 416 行删除已执行，是否需要保留更久的备份 -- 其余的保留吧。一定阶段后导出存到VM硬盘里。也在本机存一份
### 6. 前端 5 个 tab 的设计一致性。-- 视觉我会先扔给claude design做设计优化，你帮我给一个task describe文件

`index.html`/`lab.html` 是不同 agent 各自实现的。**复查结果比预期好**：核对
过 CSS 变量，`--gold`/`--green`/`--indigo` 等色值完全一致——lab.html 是照着
index.html 的设计系统做的。但**没有共享的 CSS 文件**（各自内联样式，改一处
配色要两处改）。`eval.html` 交付后建议做一轮统一视觉复核 + 抽取共享样式表
（`web/assets/app.css` 路径已预留），等三个页面都定稿后抽取一次比较省事。

---

### 4b. 【已完成，供你知情】description_long 精简 + 模型分级的真实测试结果

响应你的要求"精简description_long...只保留中文...不要都用5.4这个贵的模型"。
真实样本测试（VM 上跑真实 OpenAI 调用，非模拟）：

**description_long 精简**：`description_long_en` 整个字段砍掉（schema 层面移除，
不是留空）；`description_long_zh` 按 event_tier 分档控字数（S/A ≤300字，B ≤160字，
C/D ≤80字）。gpt-5.4 对这个分档指令服从得很好——D 档新闻实测从旧版 158-182 字
压到 58-81 字。**一个真实踩坑**：第一版 prompt 用"先在心里定好 event_tier"这种
"先做A再做B"的过程式措辞，降级模型（nano）会把这句话本身当内容写进正文（比如
真的输出"先定event_tier：MEME板块出现真实交易异动...因此取B档"），已经改成直接
说结果而非过程，重测 8 个 nano 样本无一再泄漏。

**模型分级**：只把 `market_signal`（行情异动播报）和 `calendar`（CoinMarketCal
日历条目）这两类**模板化、无需谣言甄别/事件定性判断力**的条目路由到
`gpt-5.4-nano`（输入价 $0.20/M、输出价 $1.25/M，比 gpt-5.4 便宜 12 倍）。

**我的判断和已知局限**：
1. 没有用"信源权威度低"作为降级依据——权威度低的内容（匿名X贴、聚合搜索结果）
   恰恰最需要判断力去甄别谣言/定性，权威低不等于任务简单，两者不能划等号，
   降级到弱模型反而可能放大这类内容的误判风险。只挑真正模板化、低歧义的条目类型。
2. 实测这两类条目占比不大：VM 库里 980 条事件中约 4.6%（45条）。这意味着模型
   分级本身省的钱有限，**大头的省钱来自 description_long 精简本身**（几乎覆盖
   100% 的调用，尤其是 C/D 档占比约 81% 的多数事件）。
3. nano 对字数上限的服从度不如 gpt-5.4 精确——B/C 档目标 ≤160/≤80 字，nano 实测
   常在 120-180 字（比 gpt-5.4 松，但仍比旧版不分档的 200-400 字短很多，且 nano
   单价够低，即使字数略超，实际花费仍明显低于同等长度用 gpt-5.4 生成）。内容本身
   没有发现事实错误或明显质量下降，只是没卡满目标字数。**没有加代码层强制截断**
   （怕把正常内容切成半句话），先接受这个"方向对但不够精确"的结果，如果后续正式
   跑量觉得有必要再加。



### 1. 内容理解标签让单条 LLM 成本涨了 30%，要不要接受  -- 要，现在成本大概多少
### 2. API 生产化程度够不够   -- 够用。token生成5个不同的吧，我后面可以给不同的人

### 3. X API 用量策略要不要现在重构  -- 先简单重构一下，降低点成本。昨天用了30$了。
### 4a. AB 对比工具的三个已知局限（Agent 交付时自评，非阻断项） 

评测工具合并进主页 + AB对比子tab（任务 #31）已交付并在 VM 验证跑通。交付 agent
自己指出三点，记录在这里供你判断要不要现在处理：  -- 不需要现在处理
### 5. 存量 416 行删除已执行，是否需要保留更久的备份 -- 其余的保留吧。一定阶段后导出存到VM硬盘里。也在本机存一份
### 6. 前端 5 个 tab 的设计一致性。-- 视觉我会先扔给claude design做设计优化，你帮我给一个task describe文件

3. nano 对字数上限的服从度不如 gpt-5.4 精确——B/C 档目标 ≤160/≤80 字，nano 实测
   常在 120-180 字（比 gpt-5.4 松，但仍比旧版不分档的 200-400 字短很多，且 nano
   单价够低，即使字数略超，实际花费仍明显低于同等长度用 gpt-5.4 生成）。内容本身
   没有发现事实错误或明显质量下降，只是没卡满目标字数。**没有加代码层强制截断**
   （怕把正常内容切成半句话），先接受这个"方向对但不够精确"的结果，如果后续正式
   跑量觉得有必要再加。 -- 可以的 现在的字数只是个大概描述，不是硬性标准


4. 真实的、经过验证的 $ 节省数字，要等下一轮正式 12h pipeline 跑完才能从
   `usage_tracker` 的 `by_model` 分桶里拿到确切数字（我已经加了这个分桶字段），
   现在给的都是几条真实样本的方向性验证，不是最终数字。

## 已经决定、不用再讨论的（供你确认我没记错）

- 板块标签"真相关才打"—— 已用 `anchor` 锚点强制量化，见任务 #22
- Sector Insight 板块推荐策略本身暂缓，先把数据源搞干净
- 不主动改动服务器安全配置（sshd/防火墙等）
- 运行频率：主 pipeline 12h/次（2次/天）；免费源高频存档 2h/次；X 维持随主
  pipeline 节奏，不单独提速
- 个性化因子（用户持仓相关）——Sector Insight 原始方案文档自己也写"本期没有，
  后续可加"，不是当前缺口

---

## 优化建议（我主动想到的，供参考）

### A. 存档表 `raw_items_staging` 需要清理策略

高频存档 cron 每 2 小时跑一次，`consumed_at IS NULL` 的行会累积到主 pipeline
消费。但**已消费的行（`consumed_at` 不为空）目前永远保留**，长期会让表越来越
大。建议加一个定期清理（比如保留 30 天已消费记录，更早的删掉），可以晚点做，
不紧急。

### B. `pipeline_runs` 表的历史 token 数据全是 0，是死锁 bug 导致的假象

在 UsageTracker 的 RLock 修复之前，所有历史轮次的 `llm_input_tokens` 等字段
都是 0——不是真的没有花费，是死锁导致这段代码从未成功执行过（`log_summary()`
和 `record_run(usage=...)` 都卡在死锁上，但不影响主流程，所以问题一直没被
发现）。修复后本次触发的验证性全量运行是第一次会有真实 token 数据的记录。
**用 `scripts/usage_monitor.py` 看历史趋势时，注意这个修复时间点之前的数据
没有参考价值。**

### C. 建议尽快看一眼今晚触发的验证性全量 pipeline 运行结果

为了在你睡着的时候验证多个 agent 并发改动 `pipeline.py`/`storage.py`/
`scoring.py` 后系统还能跑通，我主动提前触发了一次真实全量 pipeline（成本
与等到 00:00 UTC 的正常 cron 相当，只是提前了）。运行结果我会在这份文档或
`docs/WORKLOG.md` 里更新，你醒来后建议先看一眼这次运行是否成功、新字段
（市值标签、内容理解标签、真实性校验）是否都正确落库。

---

## 7.（2026-07-26 Fable 批次）全量代码 review 的结果与处置

15 个并行 agent 审查五个区域 → 48 条去重后发现 → 10 条高优先级进对抗验证
（9 confirmed / 1 refuted）。完整原始结果存档在 workflow 转录里，这里记处置。

### 已当场修复并部署（8 项，全部经真实运行验证）

1. **[HIGH] staging 先标记后处理**：条目在 LLM 处理前就被标记 consumed 并
   commit，pipeline 中途崩溃=整批永久丢失（无任何复活路径）。已改为
   fetch（只读）+ 写库成功后 mark，中途崩溃下一轮原样重取。
2. **[HIGH] LLM 全军覆没静默成功**：OpenAI 全面故障时 enriched=[]，pipeline
   以 success + 0 事件收场，批次无声蒸发。已改为直接 raise，配合上一条，
   staging 未标记，下一轮自动重试。
3. **[HIGH] 行情信号 URL 静态 + staging 永久去重**：同一 (币,信号,方向) 第二次
   合法触发起永远被存档层吞掉，且会拿首次播报的旧数字污染 enrich 缓存。
   已在 `_item()` 单点给 URL 加小时级时间成分，7 类信号一并修复。
4. **[HIGH] 策略实验室 A 因子二次折扣**：库里 score_authority 已是折扣终值，
   lab 又调 compute_authority 再折一次，rumor/未验证事件全算错。已改用存量值。
5. **[MED] lab.html「06 历史数据」链错 hash**（#tab6→#tab5，我当天引入的）。
6. **[MED] schema.sql 基线漂移**：缺 verification 五列与 stage_timings，
   新库引导会 500/丢运行记录。已补齐（同时补建了一直缺失的 requirements.txt，
   清掉了 env.example 里误入的补丁转储）。
7. **[LOW×4 文档腐化]** dedup.py 头部仍写 0.65（实际 0.82）、x_search 配额账本
   按旧节奏记账、OI 注释写"4小时一轮"（实际 stage_fetch 2h）、接入文档写
   "每4小时更新"+"Sector Insight 未实现"（均已过时）。全部更正。
8. **[REFUTED 1 项]** 验证 agent 推翻了一条发现（证据与当前代码不符），
   未采取行动——对抗验证这道闸就是干这个的。

### 已确认但刻意不当场修的（记录判断，待你拍板或下批处理）

- **verification.py 未知信源补位成 type='rss' 拿到 0.45 分**，恰好够过孤证闸。
  真实缺陷，但改它会整体移动 VERIFIED/PROBABLE 分布——需要单独一次带
  前后对比的标定，不适合热修。
- **DC-4 只回看 72h**：日历类事件（如提前 14 天入库的解锁）到期再报会覆盖
  而非并集 sources。修法明确（指纹匹配不限时间窗），但动的是归并核心，
  单独做+验证。
- **X 条目跨轮重复进 LLM**（低频 KOL 同 5 条推文最长连付 7 天费用）。
  顺手的解法：pipeline 把 OpenAI 的结构化结果也回写 enrich 缓存，X 条目
  第二轮起自动命中——和 enrich bridge 是同一张表，改动很小，建议下批做。
- **前端三处**：Tab1 请求失败渲染成全 0 假数据；lab 筛选变更触发双重
  runCompare POST；AB 对比正式提交绕过预览端的上限校验。下批一起清。
- **eval_tools embedding 故障静默**：dedup-test 报 0 重复、ab-compare 用全零
  相似度继续烧 GSB。应改为显式报错。下批。
- **web_search 把 bitcoinist/newsbtc 列为内容农场黑名单，sources.py 却给
  它们权威 3 的 RSS 席位**——两个口径必须二选一，这是你来定的编辑政策。
- **sector_relevance Stage1 权重算术与自述矛盾**（正文关键词命中是数学
  死路径）。校准问题，等下次跑标定时一起调。
- 其余 LOW（死代码 distHtml/FACTOR_MAP/进度条 DOM、sector limit=0 放行、
  compare 空池 200 带 error、liquidation 升级豁免量纲、状态表 NOW() 时区、
  backfill 稀疏日期窗、008 迁移不幂等、003/005 注释张冠李戴、RUMOR_LOG 与
  default_tracker 无人使用）——已留档，按"顺手清理"节奏处理，不单独排期。

---

## 8. HTTPS 网址的两条升级路径 —— 已关闭（2026-07-26 Lawrence：「域名不改了 就现在这样挺好」，维持 quick tunnel 现状）

现状：`https://currencies-granted-delight-lou.trycloudflare.com` 已上线（任何人
可打开、证书有效），但**子域名是随机的，且隧道重启后会变**。你想要的
`lawrence-b9-strategy-hub` 这种固定好名字，需要下面二选一：

**路径 A（免费，10分钟）**：你在 GCP 控制台给这台 VM 加一条防火墙规则放行
80/443（VM 的服务账号权限不够，我改不了）。开通后我用 sslip.io + Caddy 把
地址做成 `https://lawrence-b9-strategy-hub.34-138-247-158.sslip.io`——固定、
带你要的名字、Let's Encrypt 真证书。缺点：域名尾巴带 IP，不算漂亮。

**路径 B（约 $10/年，最正规）**：买一个域名（比如 b9hub.com 之类），DNS 托管到
Cloudflare 免费版，我把现有隧道升级成命名隧道绑上去——地址就是
`https://你的域名`，固定、干净、且**依然不需要动 GCP 防火墙**。

我的推荐：**路径 B**。域名钱很少，效果最正规，而且命名隧道比 quick tunnel
稳定（官方支持生产使用）。你买好域名把注册商登录方式给我（或自己把 DNS 指到
Cloudflare 后给我 API token），剩下我来。
