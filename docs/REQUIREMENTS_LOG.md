# 原始需求留痕（按时间倒序）

## 2026-07-26（下午，Fable max 四项批量需求）

> 我切换到fabel最好模型。请完成：
>
> 1. 技术架构优化（之前提的能用本地claude计算 就用claude 节省虚拟机openai的计费，但是要保障稳定性。这里一定要好好改。不要改出大问题。而且不要搞坏效果，现在效果已经不错了）
> 2. 整体review项目和代码。做关键优化点扫描和优化。
> 3. 总结回顾整个项目的经验和教训，背景，写入一个md，长期记忆后面所有项目都参考这个复盘md，降本增效
> 4. 记得所有材料都要push到github里面
>
> 现在是fabel max 如果你需要ultracode请告诉我

（同批次追问：给 designer 的 task describe 文档——已交付 `docs/design-brief-for-visual-polish.md`；
以及"最后统计一下fable的token消耗和能力体现，我和opus/sonnet对比一下"——在最终汇报里给出。）

---

> 按用户要求：大段原始 prompt 原样保存，不转述、不总结，方便回溯"当时到底是
> 怎么说的"。最新的在最前面。与 `docs/PROJECT_PLAN.md`（reframe 后的规划）
> 和 `docs/WORKLOG.md`（跟进状态）配合使用。

---

## 2026-07-26（凌晨，工作原则）

> ps 工作原则【写入长期memory 不只是本项目】：
>
> 1. 之前提到的所有任务都要落todo表，请详细检查。及时更新状态。保障不漏
> 2. 之前所有的原始prompt 尤其是之前那一大段话一大段话的，按原始情况写入一个doc保存。按时间顺序倒序整理好。
> 3. 有需要讨论的点、权衡决策的点、模糊的、优化建议（努力思考一下）。后面都记录到一个doc里，后面提醒我来review和下判断
> 4. 一段时间（比如context window占用很长了之后）就要开始做memory的提取，踩坑点、关键注意点、待办、待check、todolist的记录，避免遗忘。

（本文档与 `docs/OPEN_QUESTIONS.md` 就是对第 2、3 条的响应）

---

## 2026-07-26（凌晨，X 接口字段需求）

> 有一个需求点：所有X 接口的信息源，都一段落具体字段。然后再数据展示里，都有一个X原贴信息的字段出来

## 2026-07-26（凌晨，成本控制，第二次提醒）

> X API的钱烧的很快。注意控制一下。不要成本过快了

## 2026-07-26（凌晨，频率权衡的解法）

> 这类你可以调整回去4小时拉一次，或者拉取完先存档，等后面数据进来了一起处理。除了X这种要钱的API接口，其它都可以更高频拉取。

## 2026-07-26（下午，导航栏返工 + 复盘要求）

> 我之前不是让你做一个侧边导航栏吗？ 为什没做，而且worklog里也没有？ 1）去做 2）复盘为啥没做，以后不要再出这个错误

（补记：这条之后，Lawrence 又问「难道是我当时提漏需求了？」——见
`docs/WORKLOG.md` 的复盘章节，结论是原话确实只说了"导航栏"没说"侧边"，
但真正的问题不在这里，而是下面那条策略实验室子tab的需求被整条漏掉了。）

## 2026-07-26（白天，策略实验室交互形态）※ 补录，当时漏记

> 策略实验室 也是改成tab的模式 不要一拉到底

（紧接着的追问）：

> 子tab

**这两条当时没有被记进本文档、也没有落进 TODO 表，导致需求整条丢失，直到
2026-07-26 下午才被发现并返工。这是本项目第一起"需求留痕纪律失效"事故，
复盘见 `docs/WORKLOG.md`。**

## 2026-07-26（凌晨，前端补充要求）

> 网页里需要的别的东西，你自行补充吧。比如现在那个信息源 和详细的 事件生产流程就做的挺好的。记得加一个导航栏。

## 2026-07-26（凌晨，五项晚间批次需求 —— 最重要的一段）

> 我去睡了。请你好好完成工作。解决几个重要的问题：
>
> 1. 保障稳定全面的召回源，去重入库落表。不要出现一下能搜出来的新闻，结果咱们的表里没有
> 2. 对召回的结果进行内容理解，打标签。实体、情绪、币种等。而且重点要打上相关板块标签（真相关才打）。其它重要的维度你可以自己设计和发挥。 注意，所有涉及具体【币】的，都加上【市场价值】和【相对于BTC的xx倍市值】标签。
> 3. 对结果进行重要性打分排序。可以参考macro insight文档里面的公式定义
> 4. 输出一个可以用的API。注意你给的API就是生产化的。
> 5. 搭建一个前端网站（看你是否可以调用claude design -- 不要黑色底 要浅色底 要有设计感。设计感是很重要的） 几个tab 包括：1）这个API服务的生成过程（流程图 分支1: 爬虫 -> 聚类& 理解 ->API服务    分支2 : 爬虫 -> 聚类& 理解 ->排序分 ->API服务  这里可以把几个AI工具/模型的协同关系也放上去 爬虫前面放上数据源分类和关键数据源 旁边标注哪些走API哪些是走别的逻辑   2）真实数据展示。表格逻辑 + app 前端样式 内容替换的 模拟器逻辑 。 还有筛选功能，肯定要能筛观测时间、板块   和  3） API接口接入说明  字段格式  案例数据 提示"if you are a developer, please reach out to lawrence zhu for API secret"    4) 评测工具，可以再给几个子tab。比如duplicate tester，上传X张新闻图片，返回是否有重复的新闻等信息，帮助整理成可粘贴表格。  还有LLM评测室，模拟不同persona的币圈用户，给出用户故事，用户上传新闻后，可以给出多维度X不同用户的评测结果，包括打分、定性评估和优化建议，比如是否能看懂之类的。 5）策略实验室。开放出不同的因子权重，比如 权威性、时效性、相关性（需要先选择相关的对象）等等，然后允许用户调控权重，来查看推荐结果的变化等。并可以做两版本对比，自动出来换手率、上升、下降case、变化summary等信息

## 2026-07-26（凌晨，原始任务计划 + reframe 要求）

> 这是原始的任务计划FYI    结合后面发送了的的变化。请好好规划一下任务。有一个小细节是 代码和资料和memory都要及时update到github里，保障协同工作
>
> 1）寻找历史文件（sector insight 评测那一次）里，回忆我们如何收集全网的crypto、美股、高度相关行业（如科技）的news。深度research并梳理有哪些数据源（比如X、币安广场、各类网站），逐个梳理数据源的接入方案、best practice，还有数据源的权威性评估、数据源的效果评估等。并进入一步制作数据抓取的skill。 tip：X的API我后面会购买，但需要节省用量的用法（搜索+RSS+API结合，API只查询高相关的材料，而不是乱爬）
>
> 2）设计对数据源进行处理的pipeline，对数据进行聚合处理。生成新闻事件，对应新闻id，title，date，time_event，time_get_data, description_short（50～100字左右）,description_long（几百字），中英文都需要，和相对于内容理解标签（比如属于哪些板块（板块标签值的集合我会提供）、涉及的crypto 币、可信度、数据源、时效性、新闻类型性等）。注意新闻事件要进行去重处理，并且一定要进行【真实性校验，避免谣言】。生成skill
>
> 3）部署到我的cloud computer里面，每4小时运行一次。注意每次捕捉几百条～几千条量级的新闻就行，目的是捕捉主流事件，而不是全面扫描（避免token消耗）。数据落表到mySQL里。并打包成API服务对外提供
>
> 4）构建一个前端网站。包括几个tab 1） Lawrence Crypto News Crawler API service 产品介绍。包括数据源、生成流程 2）Data presentation（表格形式呈现具体的data和标签，支持筛选项） 3） API service   -- 我将present给我的老板

（紧接着又补了一句）：
> 把这个原始任务也 reframe好 写到github文件里

## 2026-07-26（凌晨，追问澄清）

> 5 项（其中 1 项等你确认） --需要我确认什么？

（回答：清理存量 416 行重复数据是破坏性 DELETE 操作，等待确认后已执行，见 WORKLOG）

## 2026-07-26（凌晨，两份历史方案文档）

> @"/Users/user/Downloads/[0806][RCMD]深度解析widget+L2+-+Macro+Insight-推荐.doc" @"/Users/user/Downloads/[0806][RCMD]深度解析widget+L2+-+Sector+Insight-相关热点新闻推荐 (1).doc"
> 这两个是之前的推荐位方案文件 FYI

（提取后确认：打分公式与我们的实现完全一致；Sector Insight 文档明确写"本期没有个性化因子，后续可加"；两份文档都提到需要"建设一套稳定的评测和巡检体系"）

## 2026-07-25（晚，三个重点优化方向）

> 整体进度如何？你忘了的todo： 1）给一个API说明，http如何调用（不懂技术的人也可以通过链接调用） 2）描述下这整个task是干什么，分几步走？ 有哪些优化空间和做的事情？
> 另外 3）重点优化内容召回、真实新闻验证、入库处理。然后要用别的信息验证我们的新闻召回覆盖率，要做测试哦。

## 2026-07-25（晚，X 搜索与搜索引擎召回要求）

> 搜索功能确实要好好利用起来。X的搜索。 还有google一搜就能搜出来的news我们也得有。

## 2026-07-25（晚，成本控制 + 监控要求）

> 帮我额外加一个小monitor去看openai的key的usage和价格预估。还有把运行频率改成1天2次，降低成本。

## 2026-07-25（晚，允许 SSH 连虚拟机做行情验证时的追问）

> 行情 API 生成事件也ok。有可以直接用的行情api吗。还是要去注册/简单采买。

## 2026-07-25（晚，覆盖率漏召平台追问）

> 对照 6 个未接入平台，覆盖率 91.9% —— 漏的几乎全是 MarketWatch。-->漏的平台我们能不能直接去爬

## 2026-07-25（晚，去重问题的最初实例）

> 现在的结果是有显著问题的。我们要的是事件而不是原文，可以适当改写。那么一个最重要的事情就是去重。比如"美国比特币以太坊现货ETF净流出3.11亿美元 US Spot Bitcoin, Ether ETFs See $310.6M Net Outflows on July 24
> 美国现货BTC和ETH ETF 7月24日净流出 U.S. spot BTC and ETH ETFs post July 24 outflows" 这两条应该合并成一条。
>
> 对你总结的挺对。这里要重点修复。 H因子优化要做。 sector insight的标签先不做，我们先把数据源搞干净，再对事件加标签。 小异常也处理掉。
>
> 这三个都做：事件 ID 换成稳定指纹（主体+动作+日期，不依赖 LLM 措辞）→ 聚合换成真 embedding（可以用 OpenAI embedding 或本地 sentence-transformers）→ 入库前查库做跨轮归并
>
> 代码不清晰的地方 你可以优化结构和注释

## 2026-07-25（下午，接手项目的最初指令）

> 项目已经push到githun里了，深度阅读背景，准备接手开发工作。并帮我在mysql中调取现在存的事件库，用excel返回给我。
> github.com/lawrence-1998-jpg/binance_B9_rcmd_strategy_service-
> [文件索引表格：docs/background.md、docs/task_describe.md、docs/skill-*.md、docs/recall-fix-report.md、crawler/、api/server.py、config/.env、config/schema.sql、materials/exports/、materials/screenshots/]

## 2026-07-25（更早，环境搭建）

> 连接这个github仓库。场景一个start文件，记录今天的时间。 git@github.com:lawrence-1998-jpg/binance_B9_rcmd_strategy_service-.git

> 帮我安装claude code cli

> 我有一台虚拟机，如何才能让你链接上去呢？ 这些信息够吗？
> 公网IP：34.138.247.158 SSH： ubuntu   [password]  ssh ubuntu@34.138.247.158。 是manus帮我部署的
