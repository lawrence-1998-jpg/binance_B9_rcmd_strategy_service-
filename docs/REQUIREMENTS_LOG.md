# 原始需求留痕（按时间倒序）

## 2026-07-31（第九轮：权威度体系统一裁决）

> A.把CNBC 的硬覆盖去除。
> B 1. Benzinga…给4分吧 或者你研究一下别那么高了
> 2. X 账号…不合理，不要5分了区分度不够，赶紧全面修一下这套体系
> 3. 金色财经/CryptoBriefing…不用，权威性看的是 真权威，不是产量（…一定要像编辑一样好好地判断它的权威性，可以多用公司ai key）
> 赶紧重新生成一份文件给我，现在这个完全不能用。你要做的是，给一份 新闻源 --> 权威打分 --> 备注说明 的表给我，和一些权威性判分的逻辑skill。
> 另外，完整校验和优化现在的系统架构，不能再有bug了。并且提升稳定性和可迭代性。

交付：公式 v4 + crawler/authority_table.py 单一事实源 + prompt 动态渲染 +
QA 同步断言×5 + docs/信源权威分级表.md（自动生成）。详见 PRD-05 与 WORKLOG #89。

## 2026-07-30（第八轮：部署到Agent + 版本详情 + 防护机制）

> 1. 这里希望点击后可以看见这个版本的具体参数。还有按钮太丑了，快改。
> 2. 汇报完了。版本管理这里增加一个 "部署到agent"的选项，确认后就真的发到生产里
> 3. 把今天的所有问题总结好，到log里，包括抽象到长期记忆。从机制上再好好修复一下现在的pipeline的debug。不能再犯 把整个页面的库改乱 覆盖掉这种恐怖事件了。

交付见 WORKLOG 需求 #88（含平价调试抓出的 EVENT_COLUMNS 存量缺列 bug、
rescore 三护栏与每日备份、今日十问题全景表）。

## 2026-07-30（第七轮：排序策略配置化 + 情绪方向因子）

> 1. /engineering:system-design 情绪提取的基础分这里要做一个大的修复。就是 时间范围不要48h 而是要24h的。同时对不同市场做加权，以美股、加密为主，日韩的分数比例降低
> 2. 这里增加一个大盘情绪的因子，让我可以直接调控情绪方向
> 3. 这里增加一个存为基线的按钮，点击后弹窗提示"是否确认替换基线"。确认修改后。就把 01、02、03里的排序公式和04、05里的默认配置做更新。 那么就需要你把排序公式的参数做成真的配置化的，而不是写死的。策略实验室这里有的因子都要用到哦。【注意这里要重点策略不要有bug】

排期（用户选择）：先做实验室内的（04/05 默认配置），展示后再切生产（01/02/03）。

中途用户抓到一个真 bug：

> ？ 是不是改坏了，怎么都只有benzinga的内容了，而且都是28号的旧新闻。29、30号的没有。是因为我访问的时候正在开发吗？ 帮我好好检查修正一下

根因是实验室取数按入库时间、生产按事件日期——回填 3454 条历史数据后实验室
池子被单一信源旧闻刷满。已修（取数轴对齐生产）+ QA 断言防回归。
交付详见 WORKLOG 需求 #84。

## 2026-07-30（第六轮：接入 Massive/Benzinga 实时新闻）

> 公司又给了一个新的 massive的key。也接入吧，数据质量应该还不错
>
> benzinga 的 key
> <见 config/.env 的 MASSIVE_API_KEY，不入库>
> Ben.Y
> https://massive.com/docs/rest/partners/overview#available-endpoints
>
> 直接用 benzinga 的 realtime news 的接口

交付：crawler/benzinga_news.py（直连 `GET /benzinga/v2/news`，query param 鉴权，
90 分钟回溯窗 + staging 层 url_hash 去重，不额外维护水位线）。挂进
`fetch_global_markets_sources()`（30 分钟高频路径），staging 优先级列入
P0（`_AUTHORITATIVE_MACRO_SOURCES`）。authority 定 3，未对齐 dxFeed 的机构级
5 分——直连拿到的是时间戳可信+正文完整，不是编辑质量的跃升（Benzinga 在
`web_search.py` 域名分级里本来就是 _TIER_3），详见模块内文档字符串。
QA `_NON_AGG` 白名单加 `benzinga`。

## 2026-07-29（第五轮：市场重要性权重）

> 产生了新的问题，现在的排序结果。韩国的都排在了上面。
> 其实我们美股才是最重要的。美国的新闻才是全世界用户（币安这样全球用户）真正关注的。日韩是在剧烈波动和大事件（比如芯片暴跌，且这个是也会比较大影响美国市场的）的时候才会是大部分用户关心的新闻。
> 所以我们要调一下策略逻辑。把美国市场的重要性大大提高，日本、韩国、港股的重要性降低。最后可以在策略实验室里开放一个参数栏，有几个格子 可以调这几个市场的重要性参数这样。
> 设计好产品策略 然后动手吧

交付：PRD-04 + crawler/market_weight.py（查询时市场倍率 + cross_market 豁免）+
策略实验室第三组滑杆。查证根因是 tier 相对"自己所在市场"判定、排序却是全局的
（韩股 S 档率是美股 90 倍），不是权重没调好。详见 docs/prd/PRD-04-市场重要性权重.md
与 WORKLOG 同名章节。

## 2026-07-29（第四轮：聚合器旧闻事故 + 信源时间可信度硬闸）

> [截图：财联社 6月27日 数字服务税关税报道]
> 上述在代码里也更新了对吧。
> 还有这条新闻明显是有问题的。库里数字税征税那条，是6月底的事了，明显是旧闻。赶紧删了，并扫描类似情况做处理。
> 后面逻辑上可以是这样做：强制抓取稿件发布时间和来源数量，对于来源少、非主流媒体渠道、发布时间不是近5天的内容，做强制去除。保障新闻的真实性。可以调用公司api的强模型做强处理。对库内做全面处理
>
> （追加）可以尝试去搜索，点开信息源链接去看。可以用我本地的浏览器websearch。

交付：定位为"聚合器伪造发布时间 + 无正文导致兜底闸结构性失效"，新增
crawler/source_trust.py 硬闸（LLM 前拦截），新鲜度窗口 7→5 天，存量清理
742 条聚合器孤证，QA 加 2 条红线（104 项全绿）。清理前用搜索验证了"重要
新闻有可信信源冗余覆盖"，确认删除不丢真新闻。详见 WORKLOG 同名章节。

## 2026-07-29（第三轮追加：加速处理 + 评测报告 + 实验室时间/链接 + 放宽个股新闻定义）

> 1. 保障高优加速处理这几个信息源的数据(Bloomberg 4 个频道 + Forbes-Business + WSJ + FT,共 7 个新源)
> 2. 出一份评测报告，包括召回覆盖率、各信息源贡献率（S/A/……/整体等）、画风感受、各因子感知、是否有热门感等
> 3. 策略实验室现在功能还是不好用，需要：1）标注上事件时间 2）点开要有信息源链接
>
> （中途反馈）Cramer 个股推荐/盘中异动汇总类 --> 这些我们也要抓，放宽对新闻的定义（针对头部媒体、主流资产标的时，比如apple）。个股也可以是新闻
> FT：芯片股抛售 -->这条好像没怎么排出来。
> 基于此改造一下。然后这几类财经数据源，实时性要更强一点，半小时跑一轮吧（针对S/A级为主的媒体）

交付：发现 WSJ-Markets 是个死 feed（冻结在 2025-01-27，同一天第三次"验证方法
不够"的教训），已换真实可用域名；头部资产+头部媒体的个股新闻不再一律 C/D；
新增头部媒体专属 30 分钟抓取节奏；打分口径一致性护栏当天第三次抓到真实回归
（score_market_impact/score_quality 从未在 UPDATE 子句里刷新），已修复并固化
成结构性规则。详见 WORKLOG「WSJ死源修复 + 放宽头部资产新闻定义 + 30分钟高优
抓取 + M/A/Q刷新漏洞」。

## 2026-07-29（交付后追加：召回校验 + 信源补充 + 反转加成生产环境修复 + 榜单可用性）

> 1. 校验一下：CNBC、福布斯、bloober、xdeeper（公司API那个）的首页数据我们都召回了吗（可以用我的电脑看，如果你抓不到）。召回率是多少，首20条排出来了吗，比例多少。公司api那个数据源，实际召回了多少条，有用吗？
> 2. 新闻数据源、尤其是财经大新闻数据源，我们都抓到了嘛。是否要再补一些关键召回。都保障是高优队列在处理了吗。像binance square可以后置，信息源质量低。队列优先级根据信息源质量来
> 3. 接着稳定运行下去
> 4. 策略实验室，你倒是把 情绪标签、反转因子在榜单里加一下小tag标签之类的。在数据呈现处也加一下filter啊。不然我都不知道哪条是反转，得一条条肉眼看。注意下用户可用性好吧

交付：CNBC/dxFeed 覆盖验证充分；Forbes/Bloomberg 此前完全没有专用信源
（此前 2 条记录来自 web_search 归因，非真实订阅），已补 7 个新 RSS 源并
修正优先级白名单命名不匹配。审查代码发现生产环境 `/api/news` 一直用的是
拆分前的旧单一同向倍率，反转加成只在策略实验室推演里生效过、从未真正影响
过用户看到的排序——已修复。前端加情绪加成标签（同向☾/反转⚡）与筛选器，
tab02 与 tab05 一并覆盖。详见 WORKLOG「2026-07-29 · 召回补源 + 反转加成
生产环境未生效的修复 + 榜单标签/筛选器」。

## 2026-07-29（架构评审确认 → 进开发 → 交付）

> 方案 A(我推荐):扩充 market_scope 加上"社会信号",对外正名为「新闻类型」,做成显眼的 chip 筛选条,并在事件卡片上替掉"未标注"。 可以。比如我可以选加密新闻 美股新闻等 定义为新闻类型。
>
> "社会信号"的边界是什么? 地缘冲突、自然灾害、社会事件——是只收对市场有传导路径的就行
>
> ④ 反转信号因子我加了一个硬约束进来：加的对
>
> 其它都没问题。设计好系统，开始做开发，功能测试，产品效果测试。交付最终产品和测试结论（功能+业务效果）。
> 同时后台盯一下抓进来的事件的排队处理。可以用稍微快速模型但不影响效果的，可以用。另外在开发者咨询那里，给我配一个监控看板，抓进来的数据、当前处理速度、已入库的量、有效的量（S+A级），分整体、加密、非加密。

交付：`docs/DELIVERY-2026-07-29.md`（功能 + 业务效果双结论），实现留痕见 WORKLOG「Phase 14」。

## 2026-07-29（热度氛围改造：处理优先级 + 排序因子扩展 + 新闻类型标签）

> 这里直接加上一个type "美股/日股/宏观资本……"等等。为什么美股的信息还是那么少呢。这个热度感和cnbc（友商参考）完全不同。能不能多扫一点美股的内容，现在的扫描量是不是不够啊？加爬虫、加搜索强度。包括不是有一个付费api了嘛，可以多拉取一点数据呀
>
> 还有就是这个排序上，为了营造市场事件在头部的热度感、氛围感。我们是不是要优化一下排序逻辑。比如1）加情绪调节，如果大盘情绪负向，就集体boost 负向情绪的内容。反之也是。避免内容很平，没有感觉。【策略实验室也把这个因子加上】。2）对CNBC、ForbesNews等头部权威媒体有的内容，直接boost。加一个权威媒体boost分。还有别的可能嘛？ 我们先完成需求规划，再进开发

（用数据做了 reframe 后，Lawrence 的第二轮明确指示：）

> 1）看能不能把cnbc等权威媒体拉回来的数据插队优先处理，做个排队逻辑。加大产能。但 dxfeed的数据先不用换，个股和大盘都拉，大盘的优先处理，后面每个小时加大处理量，可以有一个比较大的底池。模型可以换成gpt5.4更快一点，效果也没差吧。
> 2）这个方案也做。[采纳了"叙事相关性而非纯同向放大"的分析] 然后这里拆分成2个因子。一个是情绪同向boost因子（加分项逻辑），一个是 反转信号boost因子（加分项逻辑）
> 3）增加广度因子
> 4）增加一个冲击力因子 比如 跌xx%。涨xx%。多个权威媒体报道。对社会经济可能会产生重大影响的事件（比如federal reserve 加息减息。描刻能够影响用户资产价格、影响用户情绪的感觉。具体的定义可以先拓展梳理一下。
>
> 所有因子做出来之后用你觉得合理的方式排序。但排不对也没关系。都放到策略实验室里（正常因子和加分项稍微分开），我自己调整。还有策略实验室里，具体的内容点击后要能看见正文和每个因子对应的分数，现在不够好用
>
> 然后加筛选器那个，还是要加的。就一套独立的新闻类型category标签。加密/美股/日股/韩股/经济政策/社会信号 等等
>
> 【先梳理好需求，规划好，我们下一步再进开发】

（规划产出：`docs/prd/PRD-03-热度氛围与排序因子扩展.md`。立论基于三个实测事实：
①301 条权威源已抓回仅 8 条被处理，抓取不是瓶颈；②us_stock 已有 207 条但 84%
是 C/D 档个股噪音，是构成问题不是数量问题；③日韩宏观 S/A 占比 20-30% 远高于
crypto 的 3.1%，证明打分逻辑本身不歧视非加密内容。据此把"加爬虫/加搜索强度"
列为**非目标**。留一个阻塞性决策点：新闻类型 category 与现有 market_scope
枚举 6/7 重合，建议扩充复用而非新建第四套分类体系。**本轮只规划，未写代码。**）


## 2026-07-28（晚，二次修正：去掉加密限制 + 提频 + 情绪门槛质疑）

> 这样大跌还是中性的嘛，为啥？
> [截图：情绪横幅显示"近48小时大盘情绪：中性 -0.01"，但驱动因素里赫然列着
> "KOSPI跌超8%触发熔断"和"日经225跌4% 铠侠跌18%"]

> 不要再强限制加密新闻了！！！ 所有美股、日韩、港股、世界经济的新闻都应该要放出来！！
> 你看CNBC这几条首页新闻，为啥咱们都没有抓到呢！ 要好好解这个问题。
> [截图：CNBC 首页，圈出"Blurred front lines: Trump to meet Zelenskyy as Ukraine,
> Iran wars collide"与"AMD, Micron and Nvidia extend losses as chip stocks get clobbered"]
>
> 另外，现在用公司key 扫描就不要2天1次了，不花自己钱（不用X接口）的情况下改成1个小时1次吧。

> 你尽量用我本地来跑就行，用本地可以调公司key的。我电脑不关

（三件事都是真 bug/真限制，不是品味问题：①情绪门槛错用 importance_score，
782 条 D 档事件均值 0.317 大量越过 0.35 门槛把两条 S 档崩盘稀释成噪音，改用
event_tier 后 -0.009→-0.154 显示"偏悲观"；②CNBC 那两条召回完全正常，实测
fetch_rss 当场就能拿到，真实原因是 3468 条积压 + 每 2 天才跑一轮，外加旧 prompt
会把它们打成 D 档，修复后分别是 us_stock/macro_policy 的 B 档；③提频到每小时前
补了单轮 400 条上限和 flock 单实例锁，否则第一次触发就会一口气吃掉 3468 条积压
并与下个整点叠跑。成本口径已如实说明：VM 连不通公司网关，靠 Mac 侧 enrich worker
走网关预处理成缓存命中来实现"不花个人账号的钱"。详见 WORKLOG #62。）


## 2026-07-28（美股/港股/日股/韩股/宏观新闻扩召回 + 情绪排序 + dxFeed 接入）

> [截图：Binance App 信息流"Binance 将 ACX、LSK 和 STX 纳入监控标签"等卡片；
> 一段 PM 群聊转发："今天上午日本跟韩國股市大跌，目前 B9 與我相關頁的内容，
> 包括早午晚報、宏觀洞察、今日頭條尚未傳出這個關鍵信息與氛圍。想確認，在新的
> 推薦策略下，是否能有效傳遞？"]
>
> 现在 新闻策略数据 Agent 的要做改造。召回：现在只爬了币圈新闻是不行的。老板特别指示说美股、港股、日韩、世界主要经济新闻（对股市、资产、价格有直接影响的，能调动情绪的东西，【用户买的是价格不是价值】）也肯定要接，而且重点新闻要重点展示，排序在前面。 但【A股不要】。比如CNBCnews等。要去爬或者去搜索，主流的采集媒体。
>
> 排序环节：也要做情绪排序，如果整个市场都是悲观大新闻，那么就要大悲观。反过来就要大主观。排序结果，尤其是首几刷要有冲击力。对权威的主流财经、股市、资本市场的权威媒体做加权。  币圈的内容和主流资本的内容做混排，这样才能服务真实世界的用户（用户必然同时关系股市大盘和币圈价格，有点像robinhood
>
> 内容理解与标签呈现上，加一个 new类型：综合、加密、美股、港股、日股、韩股、重大经济政策与信息……等
>
> 请帮我做改造。明天我要拿着去对方案。我同时去找一些可靠信源的API。

（当天用真实数据验证了这次改造直接解决了 PM 那条问题：`crawler/pipeline.py`
旧版的 GENERIC-TECH FIREWALL 会把"日经跌4%/KOSPI熔断"这类内容打成 D 档——不是
没召回到，是召回到了但被判定为"与加密无关的通用宏观新闻"而雪藏。新版本对
market_scope≠crypto 的事件启用独立的评分梯度，同一批真实数据重跑后"KOSPI跌超
8%触发熔断"变成 S 档 0.71 分、"日经225跌4%"S 档 0.63 分，和加密新闻混排进同一个
列表。详见 WORKLOG #61。）

## 2026-07-28（转发 Benzinga 数据样本 + dxFeed 试用凭据，要求核实是否可用）

> [附件 benzinga.zip：Massive 平台 Benzinga 新闻/评级/财报等接口的示例响应 JSON]
> [链接：https://massive.com/docs/rest/partners/benzinga/news]
> 用公司API可以直接用能用的最好的模型比如opus4.8，不差钱。
> 另外下面资料里可能有可以用的宏观新闻API key，请帮我看看是不是有，有的话就用。并告诉我key和网站。这是公司给的账号。
> [转发邮件：Drew Z 转发 dxFeed 技术支持的账号信息——Fundamental Service
> （tools.dxfeed.com）与 News Service（news.dxfeed.com），用户名/密码均为
> binance / ***REMOVED***]

（核实结论：`benzinga.zip` 里只有示例响应数据和字段说明，**没有真实 key**，
massive.com 文档页也没有列出 base URL 或获取 key 的方式，需要找 Massive
账号负责人另外申请。dxFeed 的凭据**是真实可用的**——实测
`https://news.dxfeed.com` 用 HTTP Basic Auth（binance/该密码）直接可查，
拉到 MT Newswires 聚合的机构级实时美股新闻，已接入 `crawler/dxfeed_news.py`。
opus-4-8 的结论沿用之前的实测：网关上经 Bedrock 通道不支持这套代码依赖的
strict json schema，即使"不差钱"也是技术硬限，不是预算问题，产线继续用
gpt-5.4。详见 WORKLOG #61。）

## 2026-07-28（Mac 挂公司 VPN，追问能否本地调网关替代 claude token）

> 可以在本地 调这个接口跑吗？ 如果可以的话 我不想花claude的token了 而是用这个接口。本地有公司VPN网关，估计可以调通

（针对上一条网关切换受阻的追问：VM 连不通网关，但 Mac 本机挂公司 VPN 能连通。
落地：改造 `scripts/local_enrich_worker.py`，把本地 `claude -p` CLI 结构化换成
直接调网关的 chat.completions（strict json schema，模型 gpt-5.4）。过程中实测
撞到网关对这把 key 的硬限——30 请求/分钟，加了线程安全的滑动窗口限流器
（`GATEWAY_RPM=25`，留 5 个余量）从源头限速，而不是靠"撞 429 重试"硬穿。
干净重跑验证 46/46 全部成功、全程无 429。详见 WORKLOG #60。）

## 2026-07-28（申请到公司 LiteLLM 网关 key，要求全面切换）

> 我跟公司申请到了一个key。立刻把这个rec hub服务改造成 都用这个key来做，包括新闻的处理，都用这个key来做，而不是用本地claude的credit了，他支持好几个模型。直接用最好的gpt模型 或者opus4.8
> key：***REMOVED***
> [附公司 LiteLLM 网关接入说明：base URL、39个可用模型清单、有效期 2026-08-03]

（实测发现网关是 Binance 内网专用地址（私网 IP + 内部 ELB），生产 pipeline
所在的 GCP 公网 VM 连不通，非防火墙问题、无法绕过；且网关上的 `claude-opus-4-8`
经 Bedrock 通道不支持本仓库依赖的 strict json_schema 结构化输出。已第一时间
回退到原个人 OpenAI 账号并用真实调用+全量 QA 门禁确认服务完全恢复，网关配置
完整保留在 `config/.env`（注释状态）供网络问题解决后随时启用。详见
WORKLOG #59。）

## 2026-07-28（评测工具完整化：Agent 管理 + 校准闭环 + 评测历史）

> 继续开发评测工具：增加一个评测agent管理的页面，管理现在的评测persona（评测agent）。支持：增删改查某个persona，可以上传文件自定义persona，对应的人格、故事、偏好、记忆、心情。把现在的5个默认persona优化成3个（保留 女白领小白 资产10万美金、软件工程师稳健投资者 资产50万美金、全职交易员激进投资者 资产400万美金） 并丰富他们的故事。   同时后台记录每次评测的结果，并提供一个输入框，能够让用户输入对此评测的comment和校准。 后台基于此不断更新3个agent对应的人格和偏好。 增加一个评测历史sheet，能看到每个agent的历史评测任务和结果。
> 研究一下，如果发现这个选项下，有需要加的feature也可以单独和我说。加上。 目标是搭建一个完整的评测工具。

（"研究后主动加的 feature" 共三项，都是"缺了这套工具就不成立"的：①批量评测
N 条新闻 × M Agent 矩阵——单条评测回答不了产品问题；②人工标注基准分与 MAE
一致性指标——校准闭环没有目标函数就是玄学，必须能证明校准让 Agent 更准了；
③Agent 主观评分 vs 生产 importance_score 的相关性——排序模型的外部效度检验，
直接回答 B9 主线问题。另外评测结果落库时记录**当时的人设版本号**，否则
"校准前 vs 校准后"的对比根本无法成立。详见 WORKLOG #58。）

## 2026-07-27（demo演示完毕，暂停X API + 降低运行频率）

> agent演示完了。暂停这个服务的增量扫描中使用X的API。轮次改为每2天扫1轮而不是4轮。策略产品工作台的服务还是要用的

（落地：新增 `X_FETCH_ENABLED` 开关（`crawler/main.py`，默认 true，VM 的 `config/.env`
已设为 false），主流水线 `fetch_x_sources()` 直接跳过整条 X 腿；crontab 从
`0 8,20 * * *` 改为 `0 8 */2 * *`；`README.md`/首屏 kicker/Dev Bill 成本表随之
更新（单轮成本从 $14.17 涨到 $28.56，因为分摊基数从"每天2轮"变成"每2天1轮"，
X 一项归零）。策略产品工作台（04/05 tab 的按需调用）不受影响，走 OpenAI 不经过
这个开关，已单独验证 systemctl/首页/接口均正常。过程中顺带发现并修复了 6 处
散落在页面各处的"每天2轮/08:00·20:00"旧文案，以及一个由此暴露出的
`nextRunLabel()` 排期计算 bug（残留的老 20:00 记录被错误地带入了新排期的"下轮"
展示）。详见 WORKLOG #57。）

## 2026-07-27（产品demo通过，产出正式研发交接PRD）

> 今天这个产品demo展示通过了。准备交给正式的开发做
> 帮我输出2个prd文档。
> 1. for数据开发。一个是 爬虫的文档，要接哪些数据源。具体要哪些原始字段。要做什么简单的预处理比如说 twitter内容要做简单的url去重。是否转发要落单独一个label。
> 2. 聚类和理解的需求（排序不用）。具体需要哪些字段。必要更新规则说明好。比如聊天记录提到的那部分

（"聊天记录提到的那部分"指与产品同事 Drew Zhu 的对话：他问"同一个事件持续在
update"的情况有没有被处理，Lawrence 回复"会的。就是事件id不变，但是标签的值
是会变化的""增量表那种逻辑。记录一个快照时间"——这段对话原文被完整写进了
PRD-02 第五节。落地：`docs/prd/PRD-01-数据抓取.md`、
`docs/prd/PRD-02-聚类与理解.md`。）

## 2026-07-27（转发折叠：多个KOL转发同一内容不算独立信源）

> [同事 Drew Zhu 在讨论评测报告时问] 这里有个问题，如果多个 kol单纯转发一个内容算独立多个信源吗？
> [Lawrence 回复] 不算
> 只算非转载内容。这点我prompt进去
> [Drew Zhu] 那我的诉求你 cover 了

> 实现一下

（落地：`crawler/verification.py` 的 `analyze_sources()` 新增转发折叠——同一事件内
多个不同 KOL 账号转发/复制同一段原文，按正文归一化聚类，每簇只保留权重最高的
一家计入独立信源。详见 WORKLOG #55。）

## 2026-07-27（真实评测：vs 线上 Binance App Macro Insight）

> [20张线上信息流截图] 这个是我们线上的所有新闻库。你帮我做个真实评测，vs我们现在的数据库。重合率如何，有多少条我们独有、有多少条线上库独有。给出一个结果指标【S/A/B级事件补充量】 和每个分级下的事件补充量如何，具体的几条case（尤其是S和A级）。给一份评估报告。我可以定量地汇报实证收益。

> 用你有的所有事件就行

> 你是用claude来做评估和出报告哈。尽量不调研openai的api

> 评估报告给pdf格式哦

（落地：`docs/eval_reports/B9_vs_线上Macro_Insight_评测报告_20260727.pdf`，详见 WORKLOG #54。
仅在生成 82 条线上标题的 embedding 时调用过一次 OpenAI API，其余匹配判断/人工复核/成文均由 Claude 完成。）

## 2026-07-27（首屏收尾：标题/业务价值/联系人/GitHub链接）

> 业务价值 这4个字大一点。
> 特殊测试阶段作为新闻事件补召 API --> 特殊测试阶段作为新闻事件补召链路的生产API。把github链接放回去。

（落地：`.bizval-k` 10.5px→14px；第二条文案精确化为"补召链路的生产 API"；
tab07 仓库卡片改回可点击 `<a href>`，直达 GitHub 仓库。）

> 这个标题改成【策略API服务：全网crypto新闻事件挖掘、理解、排序】 一行就行
> 补一栏业务价值。写
> 1）作为对比baseline，定量评估新闻类内容策略的召回率、首发率
> 2）特殊测试阶段作为新闻事件补召API
> 3）运营监控
> 3个业务价值 3行。用圆角小栏的方式轻突出呈现

> please reach out to the maintainer。 的文案 maintainer 都可以改成lawrence zhu

（落地：hero 标题改单行专用 class；新增业务价值三条圆角栏；三处
"the maintainer"/"仓库维护者" 改回 "lawrence zhu"——是对更早一次隐私处理的定向回退。）

## 2026-07-27（凌晨，指标卡错位 + 留白追问）

> 这里怎么留白变大了。指标卡为什么移位了。请全面测试在交付。如果你测不了前端。可以用我的电脑测。

（根因：给锚点条加跳转目标时误往 4 列 grid 的 `#statRow` 插了个空 `<span>`，
占掉第一格；`.wrap` 限宽 1320 在宽屏上没跟着"内容全宽"放开。两处都是我
上一批改动引入的回归，非用户描述有误。已用 1900/1280/375 三档分辨率 +
逐 tab 交互实测替代截图判断，因为浏览器截图面板在本会话里出现过陈旧帧。）

## 2026-07-27（新增信源统计 tab）

> 这里再加一个小tab是 信源统计。可以查询所有信息源。分类、分级、事件数量、网址之类的。可简单筛选。样式保持一致

（落地：新端点 `/api/source-catalog`，tab02 第三个视图。）

## 2026-07-26（深夜，设计稿差异清单确认 + 时区故障排查）

> 这里是不是没有按设计稿改成流程图
> 还有把时间也放到这个流程轴里
> 请充分梳理对照所有和设计稿的差异。和我确认。比如tab1 设计稿里让加的运行历史。点开可以查看最近3次运行的状况。

（回应：逐条比对 handoff 文档与线上实现，给出 8 项差异清单，用户批复
"1-4要。5-8不要管"。）

> 为啥这里显示unauthorized
> 把能用的secret 发给我
> 你现在是有我screenshot的权限的。请进行完整的前端测试

（根因：五个 blueprint 各自的鉴权表漏加新 token；已修复并列出全部可用 secret。）

> 7/26 20:00这个轮次没有进生产吗？
> 修复处理下。把时间对上UTC+8.

（根因：`crawler` 写入时间戳仍用 UTC，且 mysqld 到 20:38 才重启导致
`CURRENT_TIMESTAMP` 默认值也还是旧时区——"改成 UTC+8"上一轮只切了系统/
cron/MySQL，代码写入口径漏改。新增 `crawler/timeutil.py` 统一基准 +
migration 011 平移存量数据，执行前已备份。）

## 2026-07-26（深夜，观测时间筛选 + Dev Bill + 隐私处理，五项并一次提出）

> 观测时间改为单选。就按我们实际的生产时间（一天2次那个节点）。还有这里不要X2了，就原始值。claude费用你没算对，claude max top版本cost了我 $250。这个footnotes去除，整个网站不要让别人能看出来和binance员工直接相关（隐私保护）。这些字大一点。就用官方logo就行了，我这就是一个demo网站。

（落地：新增 `/api/run-nodes` 生产轮次分桶；Dev Bill 去 ×2 改实测原始值并纠正
Claude 成本量级；全站隐私处理；Built by 换官方 logo 字形。）

## 2026-07-26（深夜，追加交付门禁规矩）

> 再加一个环节：如果编码阶段整体完成了，没活干了。就进入全流程QA，验证功能可用。必须拿到no bug的测试报告，这个项目才算交付，才能给我看。

（落地：`scripts/qa_suite.py` 67 条用例 + `docs/QA_REPORT.md` 报告；首跑抓到
1 个真实扣费 bug（AB 对比空请求静默付费）已修复并加回归用例，二次执行 67/67
全绿；规矩写入长期 memory 与复盘 checklist #18。）


## 2026-07-26（深夜，实验室假相关性追问 + 完工标准批评）

> 这里为什么还是假相关性。相关性打分还是没有做吗？ 请帮我做一下。我有给过一个怎么做的skill（寻找里面关于相关性定义的部分。尽量用claude/openai普通模型、用便宜的逻辑做，大致区分度即可）。

> （语音，大意）为什么你还在做错这些事？为什么不每次都在你的网站上测试你的功能？为什么需要我来当QA？请完成整个流程——不要做了第一步就忘了第二步。功能不放上界面，数据躺在表里就是浪费钱和时间。把这个写进长期记忆，帮我省时间精力和钱。

（落地：实验室 Rel 连续分上线并浏览器实测；全站孤儿数据审计抓出 6 个零消费
字段并当场接入详情页（情绪/时间尺度/市值+BTC倍数/实体/板块相关度 chips，
浏览器验证 "BTC $1,295,307M · 100.00% of BTC · mega" 真实渲染）；完工标准
写入长期 memory 与复盘 checklist #17。）


## 2026-07-26（晚，确认平衡档 + 本机优先 + 时区 + 费用监控问题）

> 行，那就这样做吧。平衡档。  是不是现在尽量使用claude本机来跑结果。openai的kpi不怎么烧钱了。提示就剩40usd了。
>
> 这个项目的所有时间都改成utc+8

> 你能有官方接口查询到并监控我的openai key 费用吗？

（落地：worker 提速到 15 分钟/轮、100 条/批、并发 6；VM 系统/MySQL/cron 全部
切 UTC+8，cron 触发时刻不变改为 08:00/20:00 表达；官方费用接口用当前 key 实测
仍 403，需要 Admin API Key，方案见最终答复。）

## 2026-07-26（傍晚，预算放宽 + 域名定案）

> 倒也不用这么省。保障效果的话要花多少钱一天？  域名不改了 就现在这样挺好

（落地：X 搜索从省钱档 150 条/轮改为平衡档——按类别分级取数（market/macro 25
条/页、其余 75 条/页）、预算上限 1500 条/轮，全部 X 成本约 $6/天；域名维持
Cloudflare quick tunnel 现状，不做固定域名升级，OPEN_QUESTIONS §8 关闭。）

## 2026-07-26（傍晚，成本事故响应 + 决策批复 + HTTPS）

> 我又买了10USD 你省着用……

> （X 账单截图：单日 Post cost $14.88，Credit remaining $-0.11，按 Post 条数计费）
> 是按用量收费的   不影响效果的时候少用一点

> 搜索不要全砍。还是要保障一搜能出来的新闻。我们要cover

> x有给一个mcp （https://docs.x.com/tools/mcp / https://api.x.com/mcp）

> 现在好几个事情，麻烦你努力并行做。改https网站。能不能把这个网站弄成一个 常规的 网址，比如 lawrence_b9_strategy_hub

同批决策批复（对 OPEN_QUESTIONS 的逐条回复）：
> bitcoinist/newsbtc 口径冲突 —— 你去帮忙做点research。然后你定
> Momentum/Novelty 进生产排序公式 —— 不要
> DC-4 72h 归并窗问题 —— 可以（单独排一次带验证的改动）
> X 条目跨轮重复进 LLM（OpenAI 结果回写缓存）—— 要的 现在就做
> 前端三处小问题 —— 改
> eval_tools embedding 故障静默 —— 改
> 其余 LOW 级 —— 改

---

## 2026-07-26（下午，Fable max 四项批量需求）

> 我切换到fabel最好模型。请完成：
>
> 1. 技术架构优化（之前提的能用本地claude计算 就用claude 节省虚拟机openai的计费，但是要保障稳定性。这里一定要好好改。不要改出大问题。而且不要搞坏效果，现在效果已经不错了）
> 2. 整体review项目和代码。做关键优化点扫描和优化。
> 3. 总结回顾整个项目的经验和教训，背景，写入一个md，长期记忆后面所有项目都参考这个复盘md，降本增效
> 4. 记得所有材料都要push到github里面
>
> 现在是fabel max 如果你需要ultracode请告诉我

（同批次追问：给 designer 的 task describe 文档——已交付 `docs/design-brief-for-visual-polish.md`；
以及"最后统计一下fable的token消耗和能力体现，我和opus/sonnet对比一下"——在最终汇报里给出。）

---

> 按用户要求：大段原始 prompt 原样保存，不转述、不总结，方便回溯"当时到底是
> 怎么说的"。最新的在最前面。与 `docs/PROJECT_PLAN.md`（reframe 后的规划）
> 和 `docs/WORKLOG.md`（跟进状态）配合使用。

---

## 2026-07-26（凌晨，工作原则）

> ps 工作原则【写入长期memory 不只是本项目】：
>
> 1. 之前提到的所有任务都要落todo表，请详细检查。及时更新状态。保障不漏
> 2. 之前所有的原始prompt 尤其是之前那一大段话一大段话的，按原始情况写入一个doc保存。按时间顺序倒序整理好。
> 3. 有需要讨论的点、权衡决策的点、模糊的、优化建议（努力思考一下）。后面都记录到一个doc里，后面提醒我来review和下判断
> 4. 一段时间（比如context window占用很长了之后）就要开始做memory的提取，踩坑点、关键注意点、待办、待check、todolist的记录，避免遗忘。

（本文档与 `docs/OPEN_QUESTIONS.md` 就是对第 2、3 条的响应）

---

## 2026-07-26（凌晨，X 接口字段需求）

> 有一个需求点：所有X 接口的信息源，都一段落具体字段。然后再数据展示里，都有一个X原贴信息的字段出来

## 2026-07-26（凌晨，成本控制，第二次提醒）

> X API的钱烧的很快。注意控制一下。不要成本过快了

## 2026-07-26（凌晨，频率权衡的解法）

> 这类你可以调整回去4小时拉一次，或者拉取完先存档，等后面数据进来了一起处理。除了X这种要钱的API接口，其它都可以更高频拉取。

## 2026-07-26（下午，导航栏返工 + 复盘要求）

> 我之前不是让你做一个侧边导航栏吗？ 为什没做，而且worklog里也没有？ 1）去做 2）复盘为啥没做，以后不要再出这个错误

（补记：这条之后，Lawrence 又问「难道是我当时提漏需求了？」——见
`docs/WORKLOG.md` 的复盘章节，结论是原话确实只说了"导航栏"没说"侧边"，
但真正的问题不在这里，而是下面那条策略实验室子tab的需求被整条漏掉了。）

## 2026-07-26（白天，策略实验室交互形态）※ 补录，当时漏记

> 策略实验室 也是改成tab的模式 不要一拉到底

（紧接着的追问）：

> 子tab

**这两条当时没有被记进本文档、也没有落进 TODO 表，导致需求整条丢失，直到
2026-07-26 下午才被发现并返工。这是本项目第一起"需求留痕纪律失效"事故，
复盘见 `docs/WORKLOG.md`。**

## 2026-07-26（凌晨，前端补充要求）

> 网页里需要的别的东西，你自行补充吧。比如现在那个信息源 和详细的 事件生产流程就做的挺好的。记得加一个导航栏。

## 2026-07-26（凌晨，五项晚间批次需求 —— 最重要的一段）

> 我去睡了。请你好好完成工作。解决几个重要的问题：
>
> 1. 保障稳定全面的召回源，去重入库落表。不要出现一下能搜出来的新闻，结果咱们的表里没有
> 2. 对召回的结果进行内容理解，打标签。实体、情绪、币种等。而且重点要打上相关板块标签（真相关才打）。其它重要的维度你可以自己设计和发挥。 注意，所有涉及具体【币】的，都加上【市场价值】和【相对于BTC的xx倍市值】标签。
> 3. 对结果进行重要性打分排序。可以参考macro insight文档里面的公式定义
> 4. 输出一个可以用的API。注意你给的API就是生产化的。
> 5. 搭建一个前端网站（看你是否可以调用claude design -- 不要黑色底 要浅色底 要有设计感。设计感是很重要的） 几个tab 包括：1）这个API服务的生成过程（流程图 分支1: 爬虫 -> 聚类& 理解 ->API服务    分支2 : 爬虫 -> 聚类& 理解 ->排序分 ->API服务  这里可以把几个AI工具/模型的协同关系也放上去 爬虫前面放上数据源分类和关键数据源 旁边标注哪些走API哪些是走别的逻辑   2）真实数据展示。表格逻辑 + app 前端样式 内容替换的 模拟器逻辑 。 还有筛选功能，肯定要能筛观测时间、板块   和  3） API接口接入说明  字段格式  案例数据 提示"if you are a developer, please reach out to lawrence zhu for API secret"    4) 评测工具，可以再给几个子tab。比如duplicate tester，上传X张新闻图片，返回是否有重复的新闻等信息，帮助整理成可粘贴表格。  还有LLM评测室，模拟不同persona的币圈用户，给出用户故事，用户上传新闻后，可以给出多维度X不同用户的评测结果，包括打分、定性评估和优化建议，比如是否能看懂之类的。 5）策略实验室。开放出不同的因子权重，比如 权威性、时效性、相关性（需要先选择相关的对象）等等，然后允许用户调控权重，来查看推荐结果的变化等。并可以做两版本对比，自动出来换手率、上升、下降case、变化summary等信息

## 2026-07-26（凌晨，原始任务计划 + reframe 要求）

> 这是原始的任务计划FYI    结合后面发送了的的变化。请好好规划一下任务。有一个小细节是 代码和资料和memory都要及时update到github里，保障协同工作
>
> 1）寻找历史文件（sector insight 评测那一次）里，回忆我们如何收集全网的crypto、美股、高度相关行业（如科技）的news。深度research并梳理有哪些数据源（比如X、币安广场、各类网站），逐个梳理数据源的接入方案、best practice，还有数据源的权威性评估、数据源的效果评估等。并进入一步制作数据抓取的skill。 tip：X的API我后面会购买，但需要节省用量的用法（搜索+RSS+API结合，API只查询高相关的材料，而不是乱爬）
>
> 2）设计对数据源进行处理的pipeline，对数据进行聚合处理。生成新闻事件，对应新闻id，title，date，time_event，time_get_data, description_short（50～100字左右）,description_long（几百字），中英文都需要，和相对于内容理解标签（比如属于哪些板块（板块标签值的集合我会提供）、涉及的crypto 币、可信度、数据源、时效性、新闻类型性等）。注意新闻事件要进行去重处理，并且一定要进行【真实性校验，避免谣言】。生成skill
>
> 3）部署到我的cloud computer里面，每4小时运行一次。注意每次捕捉几百条～几千条量级的新闻就行，目的是捕捉主流事件，而不是全面扫描（避免token消耗）。数据落表到mySQL里。并打包成API服务对外提供
>
> 4）构建一个前端网站。包括几个tab 1） Lawrence Crypto News Crawler API service 产品介绍。包括数据源、生成流程 2）Data presentation（表格形式呈现具体的data和标签，支持筛选项） 3） API service   -- 我将present给我的老板

（紧接着又补了一句）：
> 把这个原始任务也 reframe好 写到github文件里

## 2026-07-26（凌晨，追问澄清）

> 5 项（其中 1 项等你确认） --需要我确认什么？

（回答：清理存量 416 行重复数据是破坏性 DELETE 操作，等待确认后已执行，见 WORKLOG）

## 2026-07-26（凌晨，两份历史方案文档）

> @"/Users/user/Downloads/[0806][RCMD]深度解析widget+L2+-+Macro+Insight-推荐.doc" @"/Users/user/Downloads/[0806][RCMD]深度解析widget+L2+-+Sector+Insight-相关热点新闻推荐 (1).doc"
> 这两个是之前的推荐位方案文件 FYI

（提取后确认：打分公式与我们的实现完全一致；Sector Insight 文档明确写"本期没有个性化因子，后续可加"；两份文档都提到需要"建设一套稳定的评测和巡检体系"）

## 2026-07-25（晚，三个重点优化方向）

> 整体进度如何？你忘了的todo： 1）给一个API说明，http如何调用（不懂技术的人也可以通过链接调用） 2）描述下这整个task是干什么，分几步走？ 有哪些优化空间和做的事情？
> 另外 3）重点优化内容召回、真实新闻验证、入库处理。然后要用别的信息验证我们的新闻召回覆盖率，要做测试哦。

## 2026-07-25（晚，X 搜索与搜索引擎召回要求）

> 搜索功能确实要好好利用起来。X的搜索。 还有google一搜就能搜出来的news我们也得有。

## 2026-07-25（晚，成本控制 + 监控要求）

> 帮我额外加一个小monitor去看openai的key的usage和价格预估。还有把运行频率改成1天2次，降低成本。

## 2026-07-25（晚，允许 SSH 连虚拟机做行情验证时的追问）

> 行情 API 生成事件也ok。有可以直接用的行情api吗。还是要去注册/简单采买。

## 2026-07-25（晚，覆盖率漏召平台追问）

> 对照 6 个未接入平台，覆盖率 91.9% —— 漏的几乎全是 MarketWatch。-->漏的平台我们能不能直接去爬

## 2026-07-25（晚，去重问题的最初实例）

> 现在的结果是有显著问题的。我们要的是事件而不是原文，可以适当改写。那么一个最重要的事情就是去重。比如"美国比特币以太坊现货ETF净流出3.11亿美元 US Spot Bitcoin, Ether ETFs See $310.6M Net Outflows on July 24
> 美国现货BTC和ETH ETF 7月24日净流出 U.S. spot BTC and ETH ETFs post July 24 outflows" 这两条应该合并成一条。
>
> 对你总结的挺对。这里要重点修复。 H因子优化要做。 sector insight的标签先不做，我们先把数据源搞干净，再对事件加标签。 小异常也处理掉。
>
> 这三个都做：事件 ID 换成稳定指纹（主体+动作+日期，不依赖 LLM 措辞）→ 聚合换成真 embedding（可以用 OpenAI embedding 或本地 sentence-transformers）→ 入库前查库做跨轮归并
>
> 代码不清晰的地方 你可以优化结构和注释

## 2026-07-25（下午，接手项目的最初指令）

> 项目已经push到githun里了，深度阅读背景，准备接手开发工作。并帮我在mysql中调取现在存的事件库，用excel返回给我。
> github.com/lawrence-1998-jpg/binance_B9_rcmd_strategy_service-
> [文件索引表格：docs/background.md、docs/task_describe.md、docs/skill-*.md、docs/recall-fix-report.md、crawler/、api/server.py、config/.env、config/schema.sql、materials/exports/、materials/screenshots/]

## 2026-07-25（更早，环境搭建）

> 连接这个github仓库。场景一个start文件，记录今天的时间。 git@github.com:lawrence-1998-jpg/binance_B9_rcmd_strategy_service-.git

> 帮我安装claude code cli

> 我有一台虚拟机，如何才能让你链接上去呢？ 这些信息够吗？
> 公网IP：34.138.247.158 SSH： ubuntu   [password]  ssh ubuntu@34.138.247.158。 是manus帮我部署的
