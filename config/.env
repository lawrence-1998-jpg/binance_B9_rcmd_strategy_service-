# 币圈新闻爬虫环境变量
# 注意：此文件包含敏感信息，不要提交到 Git

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=***REMOVED***
MYSQL_DATABASE=crypto_news

# OpenAI (Manus 内置 LLM Proxy，自动注入)
# Manus 内置 LLM Proxy（已配置，直接使用）
# ofox.ai OpenAI 兼容聚合接口
# ============================================================
# 2026-07-28：切到公司 LiteLLM 网关（Lawrence 申请到的 key，1000 美元额度）。
# 生产流水线/评测工具/persona 校准全部靠 OPENAI_API_KEY + OPENAI_API_BASE
# 这两个环境变量路由（代码本身零改动——client 构造早就是从 env 读的），
# 所以这一行切换就让 pipeline.py / dedup.py / sector_relevance.py /
# eval_tools.py / persona_tools.py 全部走网关计费，不再走个人 OpenAI 账号，
# 也不再吃 Mac 本地 Claude Max 订阅额度（enrich worker 已同步 unload，见
# launchctl）。
#
# ⚠️ 网关 key 有效期到 2026-08-03（申请时就写明，非我方设置）——不到一周后
# 到期，到期即 402/403，流水线会整体失败。届时要么找网关方续期，要么把下面
# 两行的注释和正式行互换，回退到个人 OpenAI 账号（几秒钟的事）。
#
# 模型仍用 gpt-5.4（未换成更好的型号）：实测 claude-opus-4-8 在这个网关上
# 走 Bedrock 通道，不支持 response_format=json_schema 的 strict 模式
# （报错 output_config.format: Extra inputs are not permitted）——这套代码
# 从 pipeline 结构化到评测工具的每一处 LLM 调用全部依赖 strict JSON schema，
# 换 opus 需要先把这些调用点改造成 tool-calling 形式的结构化输出，属于更大
# 改动，非立刻切换范围内，如需要请另起需求。gpt-5.6-luna/sol/terra 三个
# 网关文档自己都标注疑似同代不同调优分支，具体差异待确认，产线不动这三个
# 未经验证的型号，仍用已经过这整套 schema 实测的 gpt-5.4。
# embeddings 已验证：网关的 text-embedding-3-small 支持 dimensions=256 参数，
# 与现有 1300+ 条事件的向量空间兼容（不会打乱去重）。
#
# 回退：把下面 OPENAI_API_KEY / OPENAI_API_BASE 两行注释掉，取消注释再往下
# 的两行「旧个人账号」即可，不用改代码、不用重新部署。
# ============================================================
# OPENAI_API_KEY=***REMOVED***  # 2026-07-28 已回退：网关是内网专用地址（172.21.x.x，内部 ELB），GCP VM 连不通，见 WORKLOG
# OPENAI_API_BASE=https://litellm.devfdg.net/v1
# 旧个人 OpenAI 账号（已临时恢复用，见上）：
OPENAI_API_KEY=***REMOVED***
OPENAI_API_BASE=https://api.openai.com/v1

# X API (购买后填写)
X_BEARER_TOKEN=***REMOVED***
X_FETCH_ENABLED=false

# CoinMarketCap API (免费层)
COINMARKETCAL_API_KEY=***REMOVED***

# API 服务配置
API_HOST=0.0.0.0
API_PORT=8080
API_SECRET_KEY=***REMOVED***
